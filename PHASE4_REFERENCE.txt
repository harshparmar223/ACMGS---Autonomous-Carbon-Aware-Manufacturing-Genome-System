"""
PHASE 4 QUICK REFERENCE
=======================

FILE STRUCTURE:
src/prediction/
  ├── __init__.py
  └── predictor.py          ← Main implementation

OUTPUT FILES (models/saved/):
  ├── predictor.pkl              Trained model
  └── predictor_metrics.pkl      Performance metrics

MODEL ARCHITECTURE:
  Input:   Genome vectors (N × 25) from Phase 3
  Model:   XGBoost or RandomForest (multi-target)
  Output:  Predictions (N × 3)
            ├─ Yield (0.5-1.0)
            ├─ Quality (0.3-1.0)
            └─ Energy (50-500 kWh)

KEY FUNCTIONS:
  load_genome_features()      - Load genome vectors from Phase 3
  load_target_values()        - Load targets from Phase 1
  split_train_test()          - 80/20 train/test split
  create_predictor_model()    - Create XGBoost/RF model
  train_model()               - Train on data
  evaluate_model()            - Compute MAE, RMSE, R², MAPE
  save_model()                - Save to disk
  load_model()                - Load from disk
  predict()                   - Make predictions
  run_prediction_pipeline()   - Complete pipeline

METRICS:
  MAE  - Mean Absolute Error
  RMSE - Root Mean Squared Error
  R²   - R-squared (0-1, higher better)
  MAPE - Mean Absolute Percentage Error

EXPECTED PERFORMANCE:
  Overall R² > 0.90 (excellent)
  Training time: ~5-10 seconds
  Prediction: <1ms per sample

TESTING:
  py verify_phase4.py    # Code quality (no deps)
  py test_phase4.py      # Full validation

USAGE EXAMPLE:
  from src.prediction.predictor import run_prediction_pipeline, predict
  
  # Train model
  model, metrics, X_test, y_test, y_pred = run_prediction_pipeline()
  print(f"R² Score: {metrics['overall']['R2']:.4f}")
  
  # Make predictions
  new_predictions = predict(model, new_genome_vectors)

INTEGRATION:
  Input from Phase 3: genome_vectors.npy
  Input from Phase 1: batch_data.csv (targets)
  Output to Phase 5: predictor.pkl (fitness evaluator)

NEXT PHASE:
  Phase 5: Use predictions for evolutionary optimization
"""
